# Color Tokens

## Source

- PDF/Figma pages:
- Export or screenshot file:

## Token List

- `brand.primary`:
- `brand.primaryStrong`:
- `brand.accent`:
- `surface.page`:
- `surface.card`:
- `text.primary`:
- `text.secondary`:
- `border.subtle`:
- `state.success`:
- `state.warning`:
- `state.error`:

## Notes

- Record hex values only after checking both light and dark image areas in the design source.
- Call out gradient usage separately if the design relies on layered brand backgrounds.
